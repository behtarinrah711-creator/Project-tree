# E2E scope

Current required smoke coverage:

- Load a seeded project in the browser.
- Open the real contract form.
- Verify the form overlay is visible.
- Verify core contract fields and save action render.
- Close the form without an uncaught browser error.

Future E2E coverage should expand around projects, tasks, activities, contacts, contracts, status forms, and critical persistence flows.
