# E2E checks

Browser regression checks are run with Playwright in GitHub Actions. The first guarded path verifies that the real contract form opens, renders its required fields, and closes without uncaught browser errors on both mobile and desktop Chromium profiles.
